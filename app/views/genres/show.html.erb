<div class="container">
  <div class="homepage">
    <h1> Rotten_<span style="color:red;">Potatoes</span> </h1>
    <h2><%=@genre.name%></h2>
    <% @movies_in_genre.each do |movie| %>
      <%= link_to movie.title, movie_path(:id => movie.id) %><br>
    <% end %>
  </div>
</div>